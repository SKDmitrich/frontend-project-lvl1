name: github-actions-test

on:
  push:

jobs:
   my-job:
    runs-on: macos-latest
    steps:
      - name: npm-install
        run: |
          make install

      - name: run-linter
        run: |
          make lint
